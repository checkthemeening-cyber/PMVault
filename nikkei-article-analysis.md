# 日経新聞記事取得失敗 - 原因分析レポート

## 調査対象記事

- **URL**: https://www.nikkei.com/article/DGXZQOUC1068K0Q6A710C2000000/
- **カテゴリ**: 建設・不動産
- **タイトル**: 夏ボーナスの業種格差広がる　大手ゼネコン200万円超、鉄鋼は3%減
- **更新時刻**: 2026年7月17日 7:18
- **性質**: 会員限定記事（有料記事）

## HTML 構造分析結果

### 記事タイトル

| 項目 | 値 |
|---|---|
| **現在のパーサセレクタ** | `<h1 class="cmn-article-title">` |
| **実際のHTML** | `<h1 class="title_t3guga0">` |
| **テキスト抽出可否** | ❌ 不一致 |

**原因**: h1 タグのクラス名が `cmn-article-title` ではなく `title_t3guga0` となっている。このため、パーサーは該当タグを認識できず、タイトルが抽出されない。

### 記事本文

| 項目 | 値 |
|---|---|
| **現在のパーサセレクタ** | `<div class="article-body">` |
| **実際のHTML検索結果** | **見つかりません（0件）** |
| **実際に存在するコンテナ** | `<div class="body_bri4l2y">` / `<div class="body_b7m6we5">` など複数の body_ クラス |
| **本文抽出可否** | ❌ 致命的に不一致 |

**原因**: パーサーが探している `article-body` クラスがページに存在しない。実際には `body_` で始まるクラス名（例: `body_bri4l2y`）が使用されている。これが最大の問題で、本文がまったく抽出できない。

### リード文・サブタイトル

| 項目 | 値 |
|---|---|
| **現在のパーサセレクタ** | `<p class="cmn-article-subtitle">` |
| **実際のHTML検索結果** | **見つかりません（0件）** |
| **実際に存在するp要素** | `<p class="descriptionTitle_d1r1zct3">` など異なるクラス |
| **リード文抽出可否** | ❌ 不一致 |

**原因**: `cmn-article-subtitle` クラスが存在しない。代わりに `descriptionTitle_` のような別のクラス名が使用されている。

### 公開日時

| 項目 | 値 |
|---|---|
| **現在のパーサセレクタ** | 正規表現: `(\d{4}年\d{1,2}月\d{1,2}日)\s+(\d{1,2}):(\d{2})` |
| **実際のHTML** | `2026年7月17日 2:00` / `2026年7月17日 7:18` |
| **マッチ可否** | ✅ マッチする |

**評価**: 日付抽出はうまく機能している。正規表現が適切。

### アクセス可能性

| 項目 | 評価 |
|---|---|
| **HTTP ステータス** | ✅ 200 OK（アクセス可能） |
| **Cookie 認証** | 不要（Cookie なしでもアクセス可能） |
| **JavaScript 依存性** | ❌ あり（コンテンツは JavaScript で動的に読み込まれている） |
| **会員限定** | ✅ はい（「この記事は会員限定記事です」と表示） |

**評価**: ページにはアクセスできるが、会員限定のため本文が制限されている。実装が React などのフレームワークを使用しており、静的 HTML には本文が含まれていない。

---

## 結論

### 取得できない主な原因（優先順位順）

1. **最大の問題: `article-body` クラスが存在しない**
   - パーサーが探しているセレクタ: `<div class="article-body">`
   - 実際のページには存在しない
   - 本文がまったく抽出できなくなっている

2. **h1 タグのクラス名変更**
   - 期待: `cmn-article-title`
   - 実際: `title_t3guga0` （動的生成されたクラス名のようにも見える）
   - タイトルが抽出できない

3. **リード文セレクタの不一致**
   - 期待: `cmn-article-subtitle`
   - 実際: 存在しない / 別のクラス名が使用されている

### 副次的な要因

- **会員限定記事**: 本文コンテンツが JavaScript で動的に読み込まれており、静的 HTML には本文テキストが含まれていない（`<k-lock-banner>` という会員限定バナーが表示される）
- **マークアップの大幅な変更**: 日経新聞が最近ページの実装を変更したと考えられる。クラス名が完全にリセットされており、古いセレクタが機能しなくなっている

---

## 推奨される修正

### 短期対応（パーサー修正）

1. **h1 セレクタの更新**
   - 現在: `<h1 class="cmn-article-title">`
   - 修正案: `<h1 class="title_t3guga0">` または `<h1>` タグを制限なく拾う

2. **本文コンテナの探索アルゴリズムの変更**
   - 期待: `article-body` クラス → これは見つからない
   - 修正案: `body_` で始まるクラスを含む div を探す、または複数の候補セレクタを試す

3. **リード文セレクタの柔軟化**
   - 現在: 厳密に `cmn-article-subtitle` クラスを要求
   - 修正案: 記事タイトルの直後にある p 要素を拾うなど、より柔軟なアプローチ

### 中期対応（スクリーンショット/JavaScript レンダリングの検討）

会員限定記事や JavaScript 動的読み込みに対応する場合は、以下の方法を検討：

- **Playwright / Selenium の使用**: ブラウザエンジンで完全にレンダリングしたページから抽出
- **記事キャッシュの利用**: JSON API や RSS フィードの利用（日経が提供していれば）
- **Cookie 認証の強化**: プレミアム会員 Cookie を設定して有料記事本文を取得

### 長期対応（頑健性の確保）

- **複数セレクタの並列試行**: 1つのセレクタで失敗したら別の候補を試す
- **セレクタの定期監視**: ページ構造が変わったことを検出して自動通知
- **フォールバック処理**: セレクタが失敗した場合の処理フロー明確化

---

## 技術詳細

### 現在の nikkei-news.py の問題箇所

**ファイル**: `/c/Users/check/PMVault/.claude/scripts/nikkei-news.py`

**問題行の実装**:

```python
# 行63: h1 タグのクラス判定
if tag == "h1" and "class" in attrs_dict and "cmn-article-title" in attrs_dict["class"]:
    self.in_title = True
    # → 実際のクラスは "title_t3guga0" なため、この条件は False

# 行71: article-body の探索
if tag == "div" and "class" in attrs_dict and "article-body" in attrs_dict["class"]:
    self.in_body = True
    # → "article-body" クラスが存在しないため、この条件は絶対に True にならない
```

これにより:
- `parser.title` は常に空文字列
- `parser.body_parts` は常に空リスト
- 記事エントリ作成時に「本文の取得に失敗しました」と表示される

### 実装の更新が必要な部分

1. **HTMLArticleParser.handle_starttag()** の h1 処理ロジック（63行周辺）
2. **HTMLArticleParser.handle_starttag()** の div 処理ロジック（71行周辺）
3. **HTMLArticleParser.handle_starttag()** の p 処理ロジック（67行周辺）

