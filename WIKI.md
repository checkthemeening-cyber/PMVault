# WIKI ― このVaultの運用ルール（AIが第一の読み手）

このVaultは第二の脳です。人間が書き込むだけの置き場ではなく、AIが読み手として知識を取り込み・繋ぎ・引き出す場所として設計します。動きは2つに分かれます。

- 蓄積 … ソースを取り込むと、AIが要点を抽出し、相互リンクしたwikiページに変えて保存する（LLM Wiki パターン）
- 引き出し … すべてのノートに4つの手がかりを持たせ、自然な言葉で過去の判断や経緯を引き出せるようにする

## 4つの手がかり（全ノート必須）
ノートを将来引き出すとき、人間とAIが必ず使う手がかりは4つだけです。

| 手がかり | 何か | 例 |
|---|---|---|
| type | 種類 | 会議録 / 提案 / 調査 / 意思決定 / 顧客メモ / 概念 / 人物 |
| when | 時期 | 作成日・関連する時期 |
| topic | 主題 | 案件名・取引先・テーマ |
| status | 状態 | 進行中 / 完了 / 保留 / 参照用 |

これを命名とfrontmatterに持たせます（[[_templates/命名と4手がかり]] 参照）。type/status の取りうる値の全体と種類別の追加プロパティは [[00_ナレッジ/concepts/プロパティ統制語彙]] にまとめてある。
- 命名: `YYYY-MM-DD-種類-主題.md`（日付ノート）。概念・人物の常設ページは主題名でよい
- frontmatter: type / status / date / topic / tags を必ず付ける。status が引き出しで一番効く

## コアファイル（毎回ここから読む）
- `index.md` … マスター索引。全ページのカタログ。まずここを読み、必要なページだけ開く（トークン節約）
- `40_運用/hot.md` … ホットキャッシュ。直近の文脈（~500語）。セッションの続きをここから始める
- `40_運用/log.md` … 操作ログ。取り込み・生成・更新の記録（追記のみ）
- `40_運用/overview.md` … このVault/案件の概要

## フォルダ
- `00_ナレッジ/` … 常設ナレッジの層
  - `00_ナレッジ/concepts/` … テーマ・手法・考え方の常設ページ
  - `00_ナレッジ/frameworks/` … 分析フレームワークのライブラリ（何を分析するかの型）
  - `00_ナレッジ/benchmarks/` … 再利用する数値・ベンチマークの引き出し
  - `00_ナレッジ/plays/` … 案件の進め方プレイブック（手順の型）
- `10_業界と事例/` … 業界・事例・動向の層
  - `10_業界と事例/industries/`・`10_業界と事例/cases/`・`10_業界と事例/trends/` … 業界プロファイル・事例バンク・四半期動向
- `20_案件/` … 案件・会話・意思決定の層
  - `20_案件/engagements/` … 案件ワークスペース。1案件1フォルダ（PARAのProjectsに相当）
  - `20_案件/sessions/` … `/save` で残した会話アーカイブ
  - `20_案件/decisions/` … 論点・前提・意思決定ログ
  - `20_案件/entities/` … 人・組織・トピック（`40_運用/domain.md` は私自身の特別なentity）
- `30_インプット/` … 取り込みの層
  - `30_インプット/.raw/` … 取り込み元の置き場。記事・議事メモ・資料をここに入れて ingest する
  - `30_インプット/sources/` … 取り込んだ資料から生成したwikiページ
- `40_運用/` … 運用ファイルの層。`40_運用/hot.md` `40_運用/log.md` `40_運用/overview.md` `40_運用/persona.md` `40_運用/domain.md` `40_運用/OWNERS.md`（更新責任マップ）
- `_研修/` … 研修の層。`_templates/`（雛形）は直下、`_研修/_実績/`・`_研修/_setup/`・`_研修/docs/`（実績・設定手順・案件背景）・`_研修/exercise/`・`_研修/hints/`（演習）はここ

## 引き出し方（AIの動き）
「Xについて、いつ何を決めたか時系列で」と聞かれたら、`40_運用/hot.md → index.md → 関連ページ` の順に必要な分だけ読み、type/when/topic/status で絞り、出典（元ノート）付きで筋を返す。Vaultが大きくても全ページは読まない。

## 取り込みループ
1. ソースを `30_インプット/.raw/` に入れる → `ingest` で wikiページ生成・相互リンク・index/log 更新
2. 価値ある会話は `/save` で 20_案件/sessions へ
3. 調べ物は `/autoresearch`（出典必須）で 30_インプット/sources へ
4. ときどき `lint` で孤立ノート・リンク切れ・抜け・矛盾を点検
5. `update-hot` で 40_運用/hot.md を最新の文脈に

## 使えるSkill
- 最初に一度: `/setup`（環境に合わせた初期設定・命令ログHookの登録）
- 蓄積: `/wiki`（初期化・索引整備）`ingest`（資料→wiki）`/save`（会話→ノート）`/autoresearch`（自律調査）`lint`（健全性点検）`update-hot`
- 引き出す: 自然言語で聞く（「〜について何を知ってる?」「過去の判断を時系列で」）
- 人間から引き出す: `/intake` `/interview`（AskUserQuestionで対話的に入力）
- 自分用に調整: `/tune-persona`（人格）`/update-domain`（私の情報）
- 持ち出す: `/save-setup` `/load-setup`（効いた設定を別案件で再現）

## 扱う情報のルール
- 主張には一次情報のURLと日付。確認できない事実は「未確認」と明記

## 広げる（発展編・任意）
このスターターは最小構成。用途が広がったら、必要な分だけ足す。既存の4手がかり・取り込みループは変えない。
- 同じ topic のノートが10件を超えたら [[00_ナレッジ/concepts/コンテンツ地図（MOC）]] を1枚切る（indexは目次、MOCは1テーマの地図）
- 手法を広げる … [[00_ナレッジ/concepts/PKM手法カタログ]] / 粒度の基準 [[00_ナレッジ/concepts/ノートの粒度（atomic・evergreen）]] / 語彙の固定 [[00_ナレッジ/concepts/プロパティ統制語彙]]
- コンサルで引く層 … 提案の型 [[00_ナレッジ/concepts/提案ストーリーの型]] / 例え話 [[00_ナレッジ/concepts/アナロジー設計の型]] / 業界 `10_業界と事例/industries/` ・事例 `10_業界と事例/cases/` ・数値 `00_ナレッジ/benchmarks/` ・案件 `20_案件/engagements/` ・フレーム `00_ナレッジ/frameworks/`
- チームで共有する時 … [[00_ナレッジ/concepts/共有の第二の脳]] と [[_研修/docs/運用ルール_チーム共有とガバナンス]]、更新責任は [[40_運用/OWNERS]]、信頼レイヤーは [[_templates/出所と確からしさ_テンプレ]]

---
> 出典・思想: LLM Wiki パターン（Andrej Karpathy 提唱）/ claude-obsidian（AgriciDaniel、MIT, https://github.com/AgriciDaniel/claude-obsidian ）/ 引き出す設計（@cyrilXBT https://x.com/cyrilXBT/status/2058373087330959829 、@defileo https://x.com/defileo/status/2043762213597397179 ）。研修用にMarkdownのみモードへ調整（ベクトルDB・追加プラグイン不要）。Givery によるセキュリティチェック済み。
