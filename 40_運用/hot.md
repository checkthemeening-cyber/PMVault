---
type: キャッシュ
status: 参照用
date: 2026-07-25
topic: hot
tags: [hot, nikkei-news, 自動化]
---

# hot ― ホットキャッシュ

直近の作業文脈（~500語）。セッションの続きはここから始めます。`update-hot` で上書き更新します。

## いま取り組んでいること

**日経ニュースの自動収集＋メール配信パイプラインの安定化**。Vaultのナレッジ整備ではなく `.claude/` 配下の自動化基盤を触っている。構成は2段。

1. **収集**（`nikkei-news` skill / Python）… Windowsタスクスケジューラで `NikkeiNewsMorning` 9:00・`NikkeiNewsEvening` 16:00。出力は `nikkei-news/daily/YYYY/MM/DD/{カテゴリ}.md`。6カテゴリ（資源エネルギー／建設・不動産／物流・運輸／商社・卸売り／自動車／素材）。素材は直近追加（commit `411c6b3`）。
2. **配信**（`nikkei-news-mailer.ps1`、新規・未コミット）… 9:15・16:15 に起動し、収集済みMarkdownを機械的にHTML化して Gmail SMTP で送信。Claudeは呼ばない決定的処理。morningは当日6カテゴリ全文、eveningは `## 夕刊更新` セクションのみ。アプリパスワードは DPAPI 暗号化で `.claude/scripts/secrets/` に置き、gitignore 済み。

## 直近で分かったこと

- **レース条件は実装で塞いだ**：収集完了前にメーラーが起動する事故があったため、収集ログに `nikkei-news {mode} done` が出るまで最大900秒ポーリングして待つ処理を入れた。原因はPCスリープによるタスク起動の遅延・重複。
- **7/25 morning は初回失敗→リトライで成功**：9:47の1回目は当日フォルダ未生成で送信ERROR。10:16以降は6セクション構築・送信成功。ただし10:18〜10:21に4回起動し、**同じメールが3通送られた**。重複起動は未解決。
- **7/25 evening は空送信**：10:08 に `anyContent=False`（夕刊差分ゼロ）のまま送信。「無言で失敗しない」設計通りだが、空メールを送る挙動でよいかは未判断。
- **7/25 収集結果**：当日公開記事があったのは3カテゴリのみ（資源エネルギー1件／建設・不動産1件／物流・運輸2件）。商社・卸売り・自動車・素材は0件。日経の一覧ページは「24日」形式と「9:00」形式が混在するため、時刻表記のみを当日記事と判定している。
- 過去の修正1〜10（HTMLパーサー刷新・相対URL対応・重複排除・メタテキスト除外・サマリの全記事分析・記者名除外・bot検証ページ拒否）は完了・動作確認済み。詳細は `40_運用/log.md`。

## 次にやること・宙に浮いていること

- **未コミットの変更が大量にステージ済み**（60件超）。mailer本体・skill・`settings.json`・7/20〜7/25の収集結果・ログが未commit。
- **`_研修/` 一式が削除ステージ済み**（`_実績/.setup-done` 含む）。意図的な削除なら、`index.md` から `[[_研修/docs/運用ルール_チーム共有とガバナンス]]` 等へのリンクが切れるので掃除が必要。
- `.obsidian/plugins/realclaudian/` に変更あり。CLAUDE.md の「.obsidianは触らない」に抵触するため扱いを確認したい。
- mailer の重複起動対策（ロックファイル等）。
- `40_運用/domain.md` が全項目空欄。`/update-domain` 未実行。
